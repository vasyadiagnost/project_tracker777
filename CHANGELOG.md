# Changelog

## v1.0.0 — initial GitHub-ready release

- Cleaned repository structure for public source publishing.
- Removed Python cache files and runtime database from the publication package.
- Added `.gitignore` for Python, build artefacts, local SQLite data and IDE files.
- Added `build_exe.bat` for Windows one-file `.exe` build via PyInstaller.
- Added `run_from_source.bat` for quick source launch on Windows.
- Rebuilt `README.md` with usage, build, release and privacy notes.
